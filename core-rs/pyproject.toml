[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "stillwater-core"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "stillwater_core"
