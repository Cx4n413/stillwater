#1 [ARCHITECTURE] Halting-Recurrent Evaluator (ACT-over-a-shared-block) (wild:4) -- A weight-tied recurrent core that re-reads the same position N times, learning to spend more forward passes on tactically hot positions and halt early on quiet ones.
   MECH: Replace BT4's fixed L-layer stack with one shared transformer block looped K times under an Adaptive Computation Time gate (PonderNet-style halting head). Distill from the BT4-borrowed teacher + harvest settled verdicts, with a per-position halting target supervised by how far the lattice's settled value moved from the net's first impression (already logged by the Distillery). At inference, STILLWATER reads the halting head to set per-node ply depth: cheap nodes get 1-2 loops, sharp nodes get 8+.
   HUGE: BT4 spends identical compute on a dead K+P ending and a razor-sharp sac â€” the strength ceiling is set by the HARDEST positions getting only fixed depth. Decoupling compute from parameters lets a small net reach effectively unbounded depth-of-thought exactly where it matters, the property that made tree search beat fixed eval.
#2 [ARCHITECTURE] Attack-Graph GNN with Board-Isomorphic Message Passing (wild:4) -- Evaluate over the literal piece attack/defense/pin graph instead of an 8x8 raster, so the net's inductive bias is chess relations rather than image patches.
   MECH: Build a typed multigraph: nodes = occupied squares + key empty squares; edges = attacks, defends, x-rays, pins, mobility-rays. A relational GNN (R-GCN / GATv2 with edge-type embeddings) does L rounds of message passing, pooled to WDL+policy heads. Train by distilling the BT4 teacher head-to-head on the same positions (we own the labels), then fine-tune on harvest. Edges are computed once in the Rust core's existing movegen.
   HUGE: Tactics ARE graph properties (a fork is a degree-2 attacker, a pin is a path through the king). A raster transformer must re-learn these from scratch each layer; a graph net gets them as native primitives and generalizes to never-seen material configs because it reasons over relations, not pixel positions. Higher tactical ceiling and far better extrapolation.
#3 [ARCHITECTURE] Differentiable-Memory Net Wired to the Persistent DAG (Aquifer-as-Weights) (wild:5) -- A Neural-Turing-Machine-style external memory whose slots ARE the engine's cross-game transposition DAG, so the evaluator reads/writes a career of experience at inference time.
   MECH: Add a content-addressable memory module (DNC/Memory-Network read head) to the eval head. Keys = learned position embeddings; values = settled WDL + proof flags banked from prior games (the persistent lattice / proof ledger / harvest). The forward pass attends over the top-k retrieved memories and fuses them with the bottom-up net features before the WDL head. Writes happen offline from harvest; reads happen per-node in the Rust broker.
   HUGE: A frozen net has zero episodic memory â€” it re-derives the same middlegame plan a million times and never accumulates. Differentiable memory makes the evaluator monotonically stronger the longer the engine lives, turning STILLWATER's unique persistent DAG from a search cache into a learning substrate â€” a true career-learning ceiling no static net has.
#4 [ARCHITECTURE] NNUE-Deep Residual Fusion (incremental trunk + deep refiner) (wild:3) -- Fuse an incrementally-updated NNUE feature trunk with a deep transformer refiner so the net is both 100x cheaper per node AND deeper than BT4.
   MECH: Maintain an NNUE-style accumulator (HalfKAv2 features) updated incrementally per move in the Rust core. Feed the accumulator as a dense 'precomputed embedding' into a small deep refiner (a few transformer/MLP-mixer blocks) that adds nonlinear global reasoning the linear NNUE lacks. Distill the whole thing from the BT4 teacher; the accumulator does the heavy lifting, the refiner adds depth, and incrementality makes per-node cost collapse.
   HUGE: STILLWATER is eval-bound at ~1291 evals/s and faster search gives ~0 Elo â€” so cost-per-eval is the binding constraint on belief resolution. A fused net that is 10-50x cheaper per node at near-BT4 quality lets the lattice settle far more nodes per move at the same wall-clock, converting raw throughput into Elo for the first time.
#5 [ARCHITECTURE] Proof-Aware Heteroscedastic Net (the net outputs its own variance, calibrated against tablebase ground truth) (wild:3) -- A net that emits not just WDL but a calibrated epistemic+aleatoric variance, trained so its uncertainty is provably honest against Syzygy and machine-checked proofs.
   MECH: Add a variance head; train with a heteroscedastic/evidential loss (e.g. Dirichlet-WDL / deep-evidential) where the supervision signal includes variance-0 anchors: Syzygy-exact positions and proven mates/draws get target variance ~0, noisy human-game positions get learned variance. The lattice backup then weights child beliefs by inverse-variance (precision-weighted fusion) instead of treating every leaf as equally trustworthy.
   HUGE: BT4 gives a point WDL with no idea when it's guessing vs knowing â€” so the lattice fuses a wild blunder-eval and a near-certain eval with equal weight, and the proof anchors STILLWATER uniquely has are wasted as mere pins. Calibrated per-node precision turns the whole belief-lattice into a proper Bayesian filter, the property that closes the search-under-extraction gap.
#6 [ARCHITECTURE] Phase/Structure Mixture-of-Experts with a Pawn-Skeleton Router (wild:3) -- Route each position to specialist sub-nets (sharp-tactical, fortress/closed, K+P endgame, opposite-bishops, etc.) chosen by a learned router over the pawn skeleton and material signature.
   MECH: A sparse MoE where a tiny router reads pawn structure + material + king safety hashes and dispatches to one of K expert FFN stacks per layer (top-1/top-2). Experts specialize via the natural clustering in the billion-game corpus and harvest; only the routed experts fire, so total params can be huge while per-node FLOPs stay BT4-class. Router can be initialized from human opening/ECO + endgame class labels.
   HUGE: A single dense net is a compromise across wildly different regimes â€” the fortress evaluations that crash engines and the deep K+P theory live in different sub-manifolds. MoE buys order-of-magnitude more effective capacity (and dedicated fortress/endgame experts) at constant inference cost, directly attacking BT4's known blind spots.
#7 [ARCHITECTURE] State-Space Sequence Model over the Game Trajectory (the net sees the whole game, not just the FEN) (wild:5) -- Evaluate positions as the latest token of the entire move sequence using a linear-time SSM, so the eval is conditioned on plan, momentum, and how the position arose.
   MECH: A Mamba/S4-style state-space model ingests the move sequence (and per-ply features) and emits WDL+policy for the current ply; the recurrent SSM state is a compressed summary of the whole game so far. Trained on the billion-game corpus (sequences are native there) plus engine games. STILLWATER carries the SSM hidden state down the lattice descent path incrementally, updating O(1) per move â€” a perfect fit for the Rust core's path-based descent.
   HUGE: BT4 is Markovian: it judges a FEN with zero memory of the plan that created it, so it can't value initiative, tempo, or a long-term squeeze that looks static. A trajectory-conditioned eval captures momentum and plan-coherence â€” strategic dimensions a position-only net structurally cannot represent â€” and the SSM's O(1) state update keeps it cheap.
#8 [ARCHITECTURE] Deep Equilibrium Evaluator (solve for a fixed-point belief instead of forward-passing) (wild:5) -- An implicit-depth net that finds the position's evaluation as the fixed point of a single layer, mirroring the lattice's own relaxation-to-equilibrium inside the network.
   MECH: A Deep Equilibrium Model (DEQ): one weight-tied transformer/GNN block iterated by a root-finder (Anderson/Broyden) to convergence z* = f(z*, position), with implicit-function-theorem gradients so training cost is one layer regardless of effective depth. Distill from BT4 to bootstrap; the number of solver iterations is adaptive per position (easy = converges in 2 steps, hard = many), giving free adaptive depth. The DEQ's own residual is a natural difficulty/uncertainty signal for the lattice.
   HUGE: BT4's reasoning depth is hard-capped at its layer count; a DEQ has effectively infinite depth, allocated adaptively, and its evaluation IS a self-consistent equilibrium â€” the same kind of object the belief-lattice relaxes toward. Network and search would share one mathematical principle (fixed-point relaxation), letting the net pre-solve locally what the lattice solves globally, a categorically different ceiling.
#9 [SEARCHLESS / DISTILLATION â€” an owned, ] Lattice-Native Distillation (train the net to be read argmax-VALUE, not argmax-VISITS) (wild:3) -- Distill SF not into MCTS-shaped policy but into a net whose value head is calibrated to be argmax-correct at every node the lattice touches.
   MECH: Take the billion SF-annotated positions; for each, the target is not just root WDL+policy but a per-legal-move value vector (SF eval after each move, mapped to WDL). Train an owned transformer with a per-move value head; the loss directly minimizes argmax-VALUE error against SF's move ranking, NOT KL-to-visit-distribution. The lattice already plays argmax-VALUE â€” so we manufacture an evaluator whose native readout IS the lattice's readout.
   HUGE: The memory pinpoints the exact bug: BT4 was trained to be read by visit-counts (a provable policy improvement) and we read it against the grain, costing ~3cp selection + tail errors. A net distilled to be argmax-VALUE-correct erases the read-against-the-grain penalty entirely â€” the single most architecture-specific gain available, unreachable with ANY borrowed net.
#10 [SEARCHLESS / DISTILLATION â€” an owned, ] Proof-Anchored Distillation (variance-0 truth bends the whole value manifold) (wild:3) -- Co-train the owned net with Syzygy/proof-ledger positions as variance-0 hard anchors so its value surface is exactly correct on the tablebase manifold and smoothly interpolates outward.
   MECH: Mix three target classes in one training run: (a) SF-soft WDL on a billion positions, (b) PROVEN terminals from 3-7man Syzygy as hard 1/0 WDL with zero label noise, (c) the engine's own proof-ledger theorems. Use a heteroscedastic loss that weights anchors by inverse-variance (âˆž for proofs). The net learns SF intuition globally but is pinned to ground truth wherever truth exists.
   HUGE: Every other distilled net treats SF eval as truth even in 6-man positions where SF is merely strong â€” our net is provably exact on the entire tablebase manifold and inherits correct gradients near it (conversion technique, fortress detection). This is a strictly-better value surface than SF itself near endgames, the regime where STILLWATER's drift was worst.
#11 [SEARCHLESS / DISTILLATION â€” an owned, ] Distill the SEARCH TREE, not the root (teacher = SF's principal variation as a trajectory) (wild:4) -- Train the net to predict SF's entire PV unrolling â€” the value at each ply of best play â€” so a single forward pass encodes calculated depth, not just a static score.
   MECH: For each position, log SF's full PV (say 20 plies) and its eval at each ply. Train multi-output heads: head_k predicts SF-value-after-k-plies-of-best-play and the move at ply k. The net distills the SHAPE of the search, not its collapse to one number. At inference the lattice reads head_0 for the static read and uses head_1..head_k as a cheap learned lookahead prior to seed which lines deserve real lattice relaxation.
   HUGE: DeepMind's searchless net distilled only ROOT eval and capped at ~2895 â€” it threw away the tree. Distilling the trajectory lets one forward pass carry depth-N tactical content, directly attacking the 'depth-findable' slice of the 7.3cp ceiling without spending lattice evals to rediscover SF's PV.
#12 [SEARCHLESS / DISTILLATION â€” an owned, ] Career Memory Net (an evaluator that learns across every game it has ever played) (wild:4) -- A small distilled net plus a frozen billion-position key/value retrieval store, where the net's value is conditioned on the nearest banked outcomes â€” and the store grows every game forever.
   MECH: The owned net produces an embedding per position; a faiss/scann index over a billion SF-annotated positions returns kNN whose SF-WDL conditions a final calibration head (learned attention over neighbors). After every real game, settled lattice verdicts + proof-ledger theorems are written into the SAME index. The net never forgets a line it has analyzed; the store is the engine's lifelong experience.
   HUGE: This breaks the frozen-weights ceiling without retraining: the engine literally gets stronger every game by accreting checked verdicts as retrievable anchors, and rare/sharp positions (the tail where it drifts) get pinned to exact neighbors instead of smoothed priors. No tree engine and no frozen distilled net can compound experience this way.
#13 [SEARCHLESS / DISTILLATION â€” an owned, ] Distribution-Native WDL Distillation (teach the FULL belief, not a centipawn point) (wild:3) -- Distill not SF's scalar eval but a full posterior over outcomes â€” including a learned variance â€” so the lattice can do true risk-aware Bayesian backup.
   MECH: Run SF (or an ensemble) at multiple depths/contempt/multipv per position; the spread of evals across these probes becomes a target VARIANCE alongside the mean WDL. The owned net gets a head that outputs (W,D,L) AND an epistemic-variance scalar. The lattice's Bayesian court and Thompson broker consume the variance natively â€” high-variance nodes get more relaxation, low-variance nodes are trusted to stop.
   HUGE: STILLWATER is distribution-native (W,D,L beliefs, posterior-sampling court) but is fed a borrowed POINT eval with no honest uncertainty â€” so its Bayesian machinery runs on a fabricated variance. A net that distills calibrated uncertainty turns the court/Thompson/should-stop logic from heuristic into principled, unlocking correct compute allocation that the memory says is the binding lever.
#14 [SEARCHLESS / DISTILLATION â€” an owned, ] Self-Distillation Flywheel (the lattice's settled verdicts teach the net to think without thinking) (wild:4) -- Use STILLWATER's own settled-minus-raw deltas (already harvested by the Distillery) as the distillation target, so the net absorbs the search's corrections and the search keeps finding new ones.
   MECH: The Distillery already logs net-first-impression vs lattice-settled verdict per position. Train the owned net to predict the SETTLED value directly â€” i.e. distill the search's fixed point into the forward pass. Iterate: stronger net â†’ deeper settled verdicts on the same budget â†’ fresh harvest â†’ retrain. SF annotations bootstrap epoch 0; the engine's own relaxation supervises all later epochs.
   HUGE: This is AlphaZero's expert-iteration loop but the 'expert' is a no-tree belief lattice with PROOFS â€” every cycle bakes calculated depth into intuition, and the corpus the memory says is 'orders too small to train from scratch' is sidestepped because we FINE-TUNE an SF-distilled base on self-generated settled targets. The flywheel has no fixed ceiling.
#15 [SEARCHLESS / DISTILLATION â€” an owned, ] Two-Brain Asymmetric Net (a tiny instant intuition + a heavy nightly verifier, distilled into each other) (wild:4) -- A fast micro-net gives instant priors for throughput; a slow deep net (or SF-d20) verifies the tail; the micro-net is continuously re-distilled from the verifier on exactly its own mistakes.
   MECH: Deploy a 10-20M micro-net co-tuned for the lattice (huge evals/s, fixing throughput so it stops mattering, per the brief). Online, the lattice flags high-disagreement / high-VOI nodes and queues them for a heavy verifier (big net or SF). Verifier answers become hard distillation targets for the micro-net via active learning â€” it is trained ONLY where it is currently wrong (the 7.3cp tail), not on the easy mass.
   HUGE: The memory says eval-quality is 'mostly cheap â€” a shallow decorrelated SF-d10 fixes ~85%' and throughput un-shelves real Elo. This makes the fast brain fast enough that search quantity is free AND surgically closes the eval tail via active learning, attacking both slices of the ceiling with one mechanism. Two-system fast/slow is exactly the brief's variant, made self-correcting.
#16 [SEARCHLESS / DISTILLATION â€” an owned, ] Counterfactual / Opponent-Conditioned Net (distill not the position's value but its value AGAINST a given player) (wild:5) -- Condition the value head on an opponent-style embedding so a single net predicts the practically-best move versus THIS opponent, not just the objectively-best.
   MECH: Train on Lichess's billions of HUMAN games keyed by rating/style, with SF providing the objective WDL and the realized human reply distribution providing the practical target. The net takes a learned opponent embedding (fed by STILLWATER's in-game Effigy rho) and outputs both objective WDL and P(opponent-error | move) â€” i.e. trap value. The lattice's Mirror/trap-band reads a learned exploitation signal instead of a hand-tuned epsilon.
   HUGE: STILLWATER already has the Effigy/Mirror/Lens machinery but feeds it a hand-built trap heuristic on an objective net. A net that has DISTILLED how millions of humans actually err converts opponent-modeling from a near-tie tiebreak into a first-class, data-grounded exploitation engine â€” the single largest source of practical Elo against fallible fields (the bot's actual opponents), invisible to objective evaluators.
#17 [DATA & SCALE â€” exploit datasets that e] Syzygy-Extrapolated Distance Field (the DTZ net that learns to count past 7 men) (wild:3) -- Train a small head on 3-7man tablebase DTZ/DTM truth to predict a continuous progress-distance for ANY position, giving the lattice the converging gradient BT4's saturated value lacks.
   MECH: Generate billions of (position, exact-DTZ, exact-WDL) labels from Syzygy (free, infinite, variance-0). Train a compact net to regress a monotone 'distance-to-conversion' scalar; supervise on 7-man truth and let it EXTRAPOLATE to 8-32 man positions where no TB exists. Feed its output ONLY as a tiebreak gradient among BT4-equal-value moves (the documented saturation/shuffle bug), never as a value rescale â€” so it cannot detune the search.
   HUGE: STILLWATER repeatedly throws dead-won games because BT4's value saturates with no gradient toward mate; a learned-from-perfect-truth distance field is the one signal that monotonically points home in every winning position, not just <=5-man TB ones. It converts the entire >7-man winning class the way TB converts the <=7-man one.
#18 [DATA & SCALE â€” exploit datasets that e] Blunder-Conditioned Opponent Field (a net of human/engine error distributions) (wild:3) -- Mine billions of Lichess games to learn P(opponent plays each reply | rating, time-pressure, position-type), turning STILLWATER's Effigy from a scalar rho into a full data-driven mistake oracle.
   MECH: From the Lichess corpus, build a conditional move-distribution model keyed on (Elo bucket, clock state, position embedding) predicting which moves real opponents actually play and where they blunder. At STILLWATER's opponent nodes (the Mirror's v_them field) back up EXPECTATION over this empirical distribution instead of assuming optimal play, steering toward lines where the specific opponent class statistically errs.
   HUGE: Against the fallible bot/human pools STILLWATER actually plays, objective best-play leaves enormous practical Elo on the table; a billion-game blunder model lets it maximize WIN PROBABILITY against a real population, not minimax value â€” a different and higher ceiling versus non-perfect opponents.
#19 [DATA & SCALE â€” exploit datasets that e] Proof-Mined Opening Tree (TB-and-SF-backed variance-0 spine grafted into the DAG) (wild:2) -- Pre-compute a massive proven/near-proven move graph from engine databases + retrograde TB analysis and seed it as pinned beliefs into STILLWATER's persistent transposition DAG before the game starts.
   MECH: Offline, run deep Stockfish + retrograde tablebase propagation over the most-visited millions of positions from engine-game databases, banking proven results and ultra-high-confidence WDL pins to the Proof Ledger format STILLWATER already loads at zero eval cost. The lattice boots with a dense variance-0 spine through known theory, spending its live GPU budget only off-book.
   HUGE: STILLWATER's ledger already re-proves banked mates at 0 evals; scaling this to a precomputed multi-million-node proven graph means it enters every game with perfect play through deep theory and reallocates its entire compute budget to the genuinely unknown frontier â€” a structural edge self-play can never accumulate.
#20 [DATA & SCALE â€” exploit datasets that e] Hardest-Positions Data Engine (active mining of STILLWATER's own worst cases) (wild:3) -- A self-amplifying loop that uses STILLWATER vs Stockfish disagreement to harvest exactly the positions where the net is currently worst, labels them with SF/TB truth, and trains on that residual.
   MECH: Stream human+engine games; for each position cheaply compare STILLWATER's belief to a Stockfish anchor; keep only high-disagreement positions (the worst cases), label them with deep SF + TB truth, and add to a residual training set. Retrain a correction head, redeploy, re-mine â€” the dataset is continuously regenerated around the current model's failure frontier.
   HUGE: Self-play saturates at ~80% draws and never visits the positions the net misjudges; an adversarially-mined corpus spends every label on the exact error surface that matters, achieving far more Elo-per-label than uniform billion-position distillation.
#21 [DATA & SCALE â€” exploit datasets that e] Move-Ranking Distillation Head (distill SF's ORDERING, the lever recal can't touch) (wild:2) -- Train a policy-correction head on billion-position Stockfish multi-PV rankings to fix BT4's move ORDER, the one signal value-recalibration provably cannot change.
   MECH: From Stockfish-annotated databases, take top-k multi-PV move orderings as a ranking target and train a small residual head that re-ranks BT4's policy prior (pairwise/listwise loss), leaving BT4's value untouched. Inject as a prior correction in selection so the lattice's branching follows SF's ordering â€” directly attacking the documented ~6.6% blind-move tail and candidate-set ceiling.
   HUGE: STILLWATER's analysis showed selection is near-optimal but the candidate SET ceiling (7.3cp) and blind-move tail are the bulk of the gap, and value-recal is monotonic so it cannot fix ordering; a ranking head is the only data-driven lever that adds the missing candidate moves without detuning the value-co-tuned search.
#22 [DATA & SCALE â€” exploit datasets that e] Decorrelated Second Net as an Information Channel (NNUE belief fused, not averaged) (wild:4) -- Train a from-data NNUE-style evaluator on the SF/human corpus whose ERRORS are decorrelated from BT4's, and fuse it as a second variance-bearing belief that disagreement makes the lattice search harder.
   MECH: Train a cheap board-seeing evaluator (NNUE/MLP) on billions of SF-labeled positions â€” a different architecture sees different things, so its mistakes decorrelate from the transformer's. In the lattice, treat the two evals as two noisy observations: agreement collapses epistemic uncertainty (stop early), disagreement raises it (search the position harder), and only fuse via uncertainty-weighting, never overwrite BT4's scale.
   HUGE: Two decorrelated weak evaluators beat one strong one when fused by uncertainty â€” STILLWATER's gap-analysis named a 'second decorrelated eval' as the eval_quality lever, and unlike value-recal this ADDS information (a new channel) rather than re-scaling the existing one, so it cannot detune.
#23 [DATA & SCALE â€” exploit datasets that e] Retrograde Conversion Curriculum (learn to win from the WIN backward) (wild:4) -- Use retrograde tablebase generation logic, scaled by SF, to build training trajectories that march outward from proven mates, teaching a net the full converging path rather than isolated position values.
   MECH: Starting from TB-proven won terminals, generate position SEQUENCES along DTZ-optimal play and outward (TB for <=7man, SF-verified for the fringe), labeling each with its exact distance-rank along a real conversion path. Train a net on these ordered trajectories so it learns the SHAPE of conversion (every step strictly progresses), then read it as STILLWATER's progress gradient in winning positions.
   HUGE: Per-position value labels (what recal tried, and failed) carry no path information; trajectory-ordered labels from perfect retrograde truth teach the net the monotone descent toward mate that is exactly what saturated BT4 lacks â€” directly curing the conversion failure mode at arbitrary material counts.
#24 [DATA & SCALE â€” exploit datasets that e] Disagreement-Triggered Deep-Verifier Cache (a growing oracle of pre-solved hard positions) (wild:4) -- Build, over the engine's career, a billion-position embedding-indexed cache of positions deeply solved by SF/TB, and at runtime substitute the cached perfect verdict whenever a near-duplicate is reached.
   MECH: Offline and continuously, deep-solve positions (SF d30+ / TB) and store (embedding -> verified WDL+best-move) in a vector index. At runtime, kNN-query the index for the current position; if a near-identical solved position exists, inject its verified verdict as a near-variance-0 pin into the lattice, otherwise fall back to BT4. The cache GROWS across the career, so the engine permanently improves on positions it or its training has already seen.
   HUGE: It gives the engine a form of permanent memory of deep truth: any position close to one already solved is answered at perfect quality for ~0 GPU, monotonically expanding the set of positions the engine plays at oracle strength â€” a compounding career asset self-play discards.
#25 [TEST-TIME ADAPTATION (never-frozen weigh] Refutation Memory (in-game error-erasing fast weights) (wild:4) -- Every refutation the search discovers this game becomes a variance-0 training example that the eval physically cannot un-learn for the rest of the game.
   MECH: A tiny LoRA-style adapter (or a key-value fast-weight bank) sits on BT4's value head, zero-init so move 1 = stock BT4. When the lattice PROVES a node (mate/repetition/Syzygy) or a deep-settled child contradicts BT4's first-impression by >threshold (logged today by the Distillery), take ONE gradient step on the adapter toward the settled WDL for that position AND its near-neighbors in embedding space. The adapter only adjusts the residual, never the global scale (regularized hard toward 0), so the search's co-tuning survives.
   HUGE: A frozen net re-evaluates the same losing structure dozens of times per game and keeps mis-reading it (the documented 'drift' / 'sawtooth' losses); this net makes a class of error AT MOST ONCE per game, converting the engine's own search into an opponent- and position-specific training signal no static net can have. The error-tail is exactly where the ~8cp/SF gap lives.
#26 [TEST-TIME ADAPTATION (never-frozen weigh] The Aquifer Prior (a career-specialized init that warms in K positions) (wild:4) -- A meta-learned base eval whose explicit job is not to be globally accurate but to be MAXIMALLY FAST to adapt â€” it reaches strong play after only K of this game's positions.
   MECH: Offline (swtrain_venv cu128 GPU), MAML/Reptile-train an adapter-init over thousands of harvested game-trajectories: each 'task' = one game, inner-loop = the Refutation-Memory updates above, outer-loop optimizes the INIT so that after K refutations the eval best matches the game's eventual settled/Syzygy truths. Ship the meta-init; at game start the adapter resets to it.
   HUGE: Decouples two things a frozen net must trade off: a static net must be a decent generalist EVERYWHERE; this net only has to be a good generalist AT the few positions it will actually adapt from, then specializes. Adaptation speed becomes the trained objective, so the in-game compounding (idea 1) pays off in 10 positions instead of 60 â€” most of a game.
#27 [TEST-TIME ADAPTATION (never-frozen weigh] Per-Opponent Career Adapters (a growing wardrobe of styles) (wild:3) -- Keep a persistent fast-weight adapter PER opponent (and per opening family), loaded the instant a known opponent appears, so the engine resumes the rivalry mid-thought.
   MECH: Hash opponent-id (lichess handle) + ECO bucket â†’ a stored adapter on disk (alongside the Proof Ledger). During a game, update it from observed opponent mistakes (the Effigy's rho already detects blunders) and from refutations of THIS opponent's pet lines. Persist at game end; reload next time. Cold opponents start from the Aquifer meta-init.
   HUGE: A frozen engine plays its 200th game vs a repeat opponent identically to its 1st, re-learning the opponent's weaknesses from zero each time and forgetting them at game end. This compounds an exploitative model across an entire CAREER â€” the trap-band/Effigy machinery already exists but is amnesiac; this gives it long-term memory.
#28 [TEST-TIME ADAPTATION (never-frozen weigh] Palimpsest-as-true-SGD (the existing hook becomes real gradient descent) (wild:3) -- Promote the already-built context-gated Palimpsest correction from a hand-crafted residual into actual backprop on a small head, driven by the lattice's settle-minus-raw residuals.
   MECH: Palimpsest currently applies context-gated eval corrections (per the README). Replace the heuristic correction with a 2-layer MLP head on the frozen BT4 embedding, and at each settle pass take a Huber-loss gradient step toward (settled_WDL âˆ’ raw_WDL) for nodes with high evidence (the same evidence>=64 filter the Distillery uses). Gate strictly to ORDERING-preserving updates: project out any gradient component that shifts the global value mean (the documented detune-killer).
   HUGE: Turns the lattice's relaxation â€” which already computes a better-than-raw value at every settled node â€” into a teacher for the raw net, in real time, on exactly the positions being searched. The search stops fighting its own raw priors; it teaches them.
#29 [TEST-TIME ADAPTATION (never-frozen weigh] Proof-Anchored Test-Time Training (only variance-0 labels touch weights) (wild:5) -- Restrict ALL in-game weight updates to come exclusively from machine-checked proofs and Syzygy hits â€” the one signal that cannot be wrong.
   MECH: Maintain a rolling buffer of (position, proven-WDL) pairs harvested THIS game from the prover bursts, repetition theorems, and tablebase conversions. Between moves (on the opponent's clock / ponder time, which the memory says is otherwise wasted), do a few gradient steps on the adapter over this buffer plus embedding-neighbors of those positions, so the certainty radiates to un-proven look-alikes.
   HUGE: Sidesteps the entire 'value-recalibration detunes' failure mode: proofs are 0/1, not a rescaled SF value, so the net learns hard FACTS (this fortress holds, this pawn race is lost) that BT4 systematically mis-judges, without shifting the soft-value distribution the search is tuned around. Endgame conversion â€” a documented chronic weakness â€” is exactly where proofs are densest.
#30 [TEST-TIME ADAPTATION (never-frozen weigh] Adversarial Self-Refutation Loop (the net learns from losing to itself, this game) (wild:5) -- Spawn a shadow search that actively HUNTS for the current eval's blind spots in the live position, and immediately patches the net against them before committing a move.
   MECH: While thinking, a parallel cheap process perturbs the eval (dropout / temperature on the adapter) to surface positions where confident-but-wrong, plays them out deeper with the full lattice to a settled/proven verdict, and feeds the disagreements back as fast-weight updates within the SAME think. The move is committed only after the eval survives its own adversary on the lines that matter.
   HUGE: A frozen net's blind spots are static and discoverable by the opponent; this net closes its own blind spots in the live tree before the opponent can exploit them â€” an active immune response per move, not passive evaluation. Directly attacks the 'settled inferior fixed point / drift' losses the gap-decomposition blamed for ~31%.
#31 [TEST-TIME ADAPTATION (never-frozen weigh] In-Context Eval (the net reads the game so far as a prompt, no weights move) (wild:4) -- Make the evaluator a sequence model whose context is the running game's settled-truth pairs, so it adapts purely by attention â€” zero gradient steps, zero detune risk.
   MECH: Train (offline, on harvested trajectories) a transformer eval that takes [the position to score] PLUS a bounded set of (earlier-position, settled-WDL/proof) tokens from THIS game, and predicts WDL conditioned on them. At inference the 'training set' is just the prompt; new in-game proofs are appended as tokens. The frozen-weights search co-tuning is untouched because no weight ever changes.
   HUGE: Gets the compounding benefit of in-game learning (idea 1) with NONE of the catastrophic-detune or stability risk that has killed every weight-changing experiment in the memory â€” adaptation is a forward pass. It can condition on opponent style and proven endgame motifs implicitly via examples rather than rules.
#32 [TEST-TIME ADAPTATION (never-frozen weigh] Lattice-Coupled TTT (the backup operator and the net co-relax to a joint fixed point) (wild:5) -- Treat the net's weights as additional lattice variables: a think iterates net-update and value-backup together until BOTH stop changing â€” a joint fixed point, not a frozen function read by a relaxation.
   MECH: Interleave the existing dirty-queue relaxation with periodic micro gradient-steps on the value head toward the current settled field (deep-equilibrium / unrolled-fixed-point style). Convergence is the joint stationary point where net-priors and lattice-beliefs are mutually consistent. Contraction is preserved by capping the per-think weight movement (the same way Î³=0.997 and capped vloss keep the value relaxation contractive); weights snap back to the Aquifer init at the next root.
   HUGE: Today the net is a fixed oracle and the lattice fights its errors forever (the 'sawtooth/drift' signature); coupling them makes the net move TOWARD the search's settled truth as the search moves toward the net's structure â€” they converge instead of oscillate. This is the deepest possible fix for 'settled inferior fixed point' because it removes the fixed-ness of the function defining the fixed point.
#33 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Residual-Memory Oracle (retrieve BT4's ERROR, not its value) (wild:3) -- Index every position where STILLWATER's settled verdict diverged from BT4's first glance, and at inference add back the retrieved correction â€” a non-parametric Palimpsest over a billion positions.
   MECH: The Distillery already logs (raw BT4 WDL/policy vs settled-lattice verdict) per root; embed each such position (BT4 penultimate-layer activations, already computed for free) into a FAISS/HNSW index keyed to its signed residual (settled âˆ’ raw) and a confidence (how converged/proven the settle was). At eval time, kNN-retrieve the k nearest *resolved* positions and add a similarity-and-confidence-weighted mean residual to BT4's raw output; proven/TB neighbors carry variance-0 and dominate.
   HUGE: The gap-attribution says the loss is a TAIL problem (median ~0cp, the 7.3cp ceiling lives in rare positions BT4 mis-judges) â€” a parametric retrain smears corrections across all weights and forgets the tail, but a memory is EXACTLY right on positions structurally like ones it already resolved, and it compounds forever as the career generates more residuals, with zero retrain.
#34 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Middlegame Tablebase by Analogy (retrieve the right endgame TB for an unsolved position) (wild:5) -- Learn an embedding under which a middlegame maps to the perfect-information tablebase position whose THEORY it shares, importing 7-man ground truth into the 32-man fog.
   MECH: Build a metric (contrastive, trained on Distillery + Stockfish-annotated Lichess) where two positions are 'near' iff their correct PLAN and result agree, not their pixels; index the FULL Syzygy 7-man set plus all resolved endgame frontiers. At a middlegame leaf, retrieve the nearest TB-grounded analog and condition BT4 on its proven WDL and winning-motif (e.g. 'this is a won opposite-bishop fortress-break'), injected as a variance-low pin.
   HUGE: Tablebases are the only source of PERFECT chess truth and STILLWATER already ingests them as variance-0 anchors at the 7-man frontier â€” analogical retrieval propagates that truth up the tree decades earlier than search can reach it, giving the engine certainty about plans (fortress/zugzwang/breakthrough) that BT4's value head only fuzzily approximates.
#35 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Plan Retrieval (retrieve the continuation skeleton, then verify it) (wild:4) -- Retrieve not a number but the multi-move PLAN that worked in the nearest neighbors, seed it as a policy prior / proof-search target, and let the lattice verify the imported idea.
   MECH: Index human+engine games by position embedding, storing the realized continuation and its eventual result; at a leaf, retrieve the k nearest positions and extract the modal move-sequence motif (e.g. the maneuver Nd2-f1-g3-f5). Inject it as a strong policy prior on exactly those moves (fixing BT4's ~6.6% blind-spot moves the gap-attribution found) AND as a proof-burst seed line, so the lattice cheaply CHECKS the borrowed plan rather than trusting it.
   HUGE: The 7.3cp candidate-set ceiling is partly DEPTH-findable: good moves at low policy rank BT4 never surfaces â€” retrieval supplies the missing candidate from a billion games of human pattern-intuition, and the lattice's proof machinery converts borrowed intuition into verified truth, marrying recall with soundness in a way neither pure net nor pure search can.
#36 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Counterfactual-Neighbor Calibration (retrieve to set DISTRIBUTION, not the mean) (wild:4) -- Use the spread of retrieved neighbors' actual results to set BT4's epistemic variance and the aleatoric W/D/L shape â€” a non-parametric posterior over outcome from a million precedents.
   MECH: For a leaf, retrieve its k nearest neighbors with their EMPIRICAL result histograms (from millions of games at that motif); the neighbor result-spread directly parameterizes the node's belief: tight cluster â†’ low epistemic (replace the hand-tuned EPI0=1.0 with a measured one), divergent cluster â†’ genuinely sharp (high aleatoric). Feed this measured spread into the VOI selection and Root Court stopping width.
   HUGE: STILLWATER's whole selection+stopping machinery (VOI, Court p_best) runs on UNCALIBRATED uncertainty constants admitted as a weakness; a billion-game precedent base gives an actual measured posterior width per position, turning the belief lattice from heuristic-variance to data-grounded Bayesian â€” the clean closure the architecture notes wished for, sourced non-parametrically.
#37 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] The Self-Indexing Career (the lattice DAG becomes the retrieval corpus) (wild:3) -- STILLWATER indexes its own persistent transposition DAG and proof ledger so every position it ever deeply settled is instantly recallable by similarity in all future games â€” institutional memory across a career.
   MECH: Persist not just exact-key transpositions but an embedding index over every node the engine ever relaxed to convergence (with its settled WDL, proof status, and the eval-budget that produced it); on a new, never-seen position, retrieve the nearest PREVIOUSLY-SETTLED nodes and warm-start the leaf belief from their converged verdicts instead of a cold single BT4 glance.
   HUGE: Today every game starts the lattice cold; a self-indexing career means the 10,000th game evaluates middlegames it effectively already 'understands' from structurally-similar past battles â€” compute spent settling a position is amortized across the engine's lifetime, an asset that strictly grows and that no frozen-weight engine can possess.
#38 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Adversarial Retrieval Hashing (index the positions where BT4 is WRONG) (wild:4) -- Train an embedding whose explicit objective is to cluster BT4's failure modes, so retrieval is densest exactly where the net is least trustworthy.
   MECH: Mine (via SF-d18/TB disagreement on Lichess) the set of positions where BT4's value is wrong by >threshold; train a metric-learning head that pulls same-failure-mode positions together and pushes correct-eval positions apart (the index is OVER-RESOLVED on errors, sparse on easy positions). At inference, retrieval-density itself is a trust signal: many error-cluster neighbors â†’ distrust BT4 here and defer to retrieved truth / spend more search.
   HUGE: This turns retrieval into a learned error-DETECTOR, not just a corrector â€” it tells the engine WHEN BT4 is in a known-blind regime (the tail), letting it route scarce compute and memory precisely to the 7.3cp ceiling positions instead of spending it uniformly, attacking the exact tail structure the gap-attribution identified.
#39 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Retrieval-Conditioned In-Context Net (RETRO for chess) (wild:5) -- Feed the retrieved neighbors and their outcomes INTO the network as extra input tokens, so the evaluator reads precedents in-context and reasons over them rather than just averaging their values.
   MECH: Train a small head (or a LoRA-style adapter on a distilled GM-transformer we own) that takes BT4's position embedding PLUS the embeddings+results of its k retrieved neighbors as a context set, and outputs a corrected WDL/policy; the net learns to weight, contradict, or combine precedents (cross-attention over the retrieved set) â€” chess's analog of RETRO/RAG language models.
   HUGE: kNN-averaging is linear and blind to WHICH neighbor is relevant to THIS position's twist; an in-context reasoner can say 'three neighbors say win but they lacked the back-rank weakness this one has' â€” it converts a billion-position memory from a lookup table into a precedent-reasoning engine, the highest ceiling of all retrieval forms.
#40 [RETRIEVAL & EXTERNAL MEMORY â€” non-para] Hierarchical Coarse-to-Fine Retrieval (structure â†’ plan â†’ move) (wild:4) -- Retrieve in three nested resolutions â€” pawn-structure cluster picks the strategic regime, plan-embedding picks the maneuver, exact-tactic embedding picks the move â€” each level's hit conditioning the next query.
   MECH: Three indices at coarsening granularity: (1) pawn-skeleton/material hash â†’ the strategic family and its long-run statistics; (2) within that family, a plan-embedding retrieves the typical winning maneuvers; (3) a fine tactical embedding retrieves exact-position refutations. Each level returns a prior that conditions the query and weighting at the next, mirroring how a human reasons from structure down to calculation.
   HUGE: Flat kNN drowns the rare-but-decisive tactical neighbor in a sea of structurally-similar-but-strategically-different positions; hierarchy lets coarse strategic truth (which is statistically abundant and reliable) gate the fine tactical retrieval (rare, high-variance), giving correct generalization at BOTH the plan and move scales â€” the structure human chess understanding actually has and that flat nets flatten away.
#41 [COGNITIVE / GRANDMASTER MODELING â€” dec] Chunk-Conditioned Eval (the De Groot / Chase-Simon retrieval layer) (wild:3) -- Recognize the position as a constellation of named human chunks (pawn skeletons, piece configs, king-safety motifs) and CONDITION the evaluator on which chunks are present, the way a GM 'sees' familiar fragments before calculating anything.
   MECH: Mine a chunk vocabulary by frequent-subgraph mining over billions of Lichess/annotated positions (local attention/co-occurrence on the 8x8 graph yields ~10^3 recurring 3-8 piece chunks); tag every harvest position with its active chunk-set; learn a small per-chunk eval-residual + per-chunk policy-bias table (variance-0 where a chunk co-occurs with a Syzygy/proof result). At inference, BT4's value/policy is composed with the retrieved chunk-residuals BEFORE the lattice backup, so the FEED is corrected, not the readout.
   HUGE: Chase-Simon showed GM superiority IS chunk recognition, not deeper raw calculation; this gives the net the GM's compressed positional vocabulary it never had, attacking the ~19% eval-blind tail at the exact granularity humans use, without retraining BT4 or touching the co-tuned readout.
#42 [COGNITIVE / GRANDMASTER MODELING â€” dec] Prophylactic Mirror â€” proving the opponent's PLAN dead, not just their move (wild:4) -- Make STILLWATER play Nimzowitsch/Karpov-style prophylaxis: explicitly infer the opponent's BEST PLAN (a multi-move schema, not one reply) and prefer moves that provably collapse the value of that plan's continuation.
   MECH: Extend the existing Effigy/Mirror (v_them field): run a short 'what does the opponent WANT' rollout in the Mirror field to extract the opponent's top schema (a sequence/target square cluster), then score our candidate moves by the DROP they induce in the Mirror-field value of that schema's terminal â€” a prophylaxis bonus folded into the our-side backup, gated like the Lens (only opens below RHO_GATE / against fallible opponents, so it never sacrifices objective value vs perfect play).
   HUGE: Engines defend reactively move-by-move; GMs win by preventing the opponent's idea two moves before it exists. A no-tree lattice already carries a settled opponent-belief field (v_them) â€” this is the ONE engine substrate that can score 'how much did I kill their plan' as a graph quantity, a defensive dimension brute eval has no representation for.
#43 [COGNITIVE / GRANDMASTER MODELING â€” dec] Two-System Candidate Proposer (System-1 intuition gates System-2 calculation) (wild:3) -- A learned proposer outputs a SMALL candidate set plus a per-candidate 'how deep must I calculate this' budget, and the lattice spends its evals only where the proposer flags deep calculation is needed â€” Kahneman's System-1 triage steering System-2.
   MECH: Train a tiny head on the harvest corpus (target: which of BT4's top-k moves SF/lc0 actually needed deep search to separate, vs. which were decided at the policy) â€” a 'calculation-necessity' classifier. At root, the lattice's broker priority is multiplied by this necessity score: quiet decided moves get capped shallow; flagged sharp moves get the whole budget. This ADDS an allocation prior to the existing broker; it does not change the backup or the readout.
   HUGE: STILLWATER spends its fixed eval budget roughly uniformly and DRIFTS by over-searching settled inferior lines; GMs allocate calculation depth wildly non-uniformly. Concentrating the budget on the few genuinely sharp moves directly attacks the 7.3cp candidate-set ceiling and the drift losses, the dominant search-side failure mode.
#44 [COGNITIVE / GRANDMASTER MODELING â€” dec] Plan-as-Subgoal: searching toward learned schemata instead of toward value (wild:4) -- Give the lattice intermediate SUBGOALS (minority attack, outpost on d5, open the f-file, trade light-squared bishops) as auxiliary value channels, so search has a positional gradient where BT4's value saturates and the engine currently shuffles.
   MECH: Mine a library of GM plans as (precondition chunk-set -> target board-predicate) rules from annotated games; learn a value-head per active plan (P(plan completes) from the harvest corpus). When BT4's value is saturated/flat (the documented conversion-shuffle regime), the broker adds a small plan-progress potential to edge priority â€” a potential-based shaping term (sound: telescopes to zero, can't change the optimum), giving direction exactly where the net gives none.
   HUGE: The single most-replicated STILLWATER failure is value-saturation drift/shuffling (won endgames, quiet maneuvering) â€” the net has no gradient, so search wanders. A learned plan-potential supplies the 'what am I trying to DO' axis that turns a flat value landscape into a navigable one, which no scalar evaluator can provide.
#45 [COGNITIVE / GRANDMASTER MODELING â€” dec] Career Schema Memory â€” a lattice that learns the GAME, not just this game (wild:4) -- A persistent cross-game memory keyed by ABSTRACT schema-embedding (not exact Zobrist) so the engine retrieves 'I have faced this TYPE of position before and here is what worked', accumulating positional wisdom across its whole career like a maturing master.
   MECH: Extend the proof Ledger / Aquifer: alongside exact-position theorems, store (schema-embedding -> empirical post-settlement value + best-plan + outcome) records harvested every think; at a new position, kNN-retrieve the nearest schema records and inject their settled value as a soft, variance-weighted prior on the root field (down-weighted by embedding distance, OVERRIDDEN by any exact proof). Grows monotonically with games played.
   HUGE: Today the lattice forgets everything between games except exact proofs; a GM's strength is decades of accumulated positional memory generalizing across similar-but-not-identical positions. This is the only path to an intelligence that gets STRONGER the more it plays without any retraining â€” career-scale generalization the frozen net structurally cannot do.
#46 [COGNITIVE / GRANDMASTER MODELING â€” dec] Calculation-Tree Imagination Head (a learned 'I see the line' trajectory generator) (wild:5) -- A sequence model that, like a GM visualizing a forcing line, GENERATES the critical principal variation in one shot, which the lattice then only has to VERIFY rather than discover by relaxation.
   MECH: Train an autoregressive 'line generator' on Stockfish/lc0 PVs and brilliant human combinations (billions available) to emit a candidate 8-16 ply forcing sequence from a position; at search time, seed the lattice by injecting that whole trajectory as high-priority edges (forced expansion) before normal relaxation â€” converting the deep-line-discovery problem (where lc0 beats us on the same net) into cheap proof-verification of a proposed line. Sound: a generated line is only a SUGGESTION; the lattice's proofs/backup still arbitrate.
   HUGE: The documented #1 search-side loss is failing to find the deep line lc0 finds on the same net (drift / structure ~63% of decisive losses). A GM doesn't relax a lattice to find a 12-ply sac â€” they SEE it and check it. Imagining the PV and verifying it directly attacks the depth-findable + structure tail that quantity-of-search provably does not fix here.
#47 [COGNITIVE / GRANDMASTER MODELING â€” dec] Critical-Moment Detector â€” the GM's sense of WHEN the game is decided (wild:3) -- A learned 'this is the critical position' signal (the move on which the whole game hinges) that triggers a qualitatively different, exhaustive verification mode â€” modeling the GM's feel for when to stop playing on intuition and burn everything.
   MECH: Train a head on annotated games (NAG '!'/'?' density, eval-swing-ahead, human clock-time spikes â€” all in the corpus) to predict criticality at the current root; when it fires, the engine escalates: runs the proof-burst prover at full depth, opens the imagination head, and lifts the hard time cap (the banked clock the engine documentedly never spends) for THIS move only. On non-critical moves it snaps fast, banking time for the moments that matter.
   HUGE: STILLWATER spends time uniformly and banks a clock it can't usefully reclaim because it has no signal for WHICH move deserves the bank; humans win by recognizing the one critical moment and pouring resources there. A learned criticality trigger finally lets the engine USE its banked time on exactly the positions where a single error decides the game â€” the tail that defines the gap.
#48 [COGNITIVE / GRANDMASTER MODELING â€” dec] Aspiration & Self-Doubt â€” modeling the GM's expectation and blunder-check reflex (wild:4) -- The engine carries an EXPECTATION of the position's value (an aspiration level) and a learned blunder-detector; a candidate that violates the aspiration or trips the detector triggers mandatory deep re-examination â€” modeling 'wait, that feels wrong, let me check again.'
   MECH: Maintain a slow-moving aspiration value (EWMA of the settled root value across the game). Train a lightweight 'regret-predictor' on harvest+SF labels: P(this move loses >=80cp under deeper search) from cheap features (value drop vs aspiration, policy-rank of the chosen move, epistemic uncertainty already in the lattice). If the about-to-be-played move trips it, force a second, decorrelated verification pass (deeper budget + the imagination head) before committing. Pure ADD-ON gate at the decision site â€” never changes the backup.
   HUGE: The gap is a TAIL of rare catastrophic moves amid near-perfect play (mean 10cp, median ~1) â€” exactly the profile a blunder-check reflex targets. Catching even half the >=80cp tail errors (1.6% of moves) before committing converts directly to Elo, attacking the failure class no amount of uniform search depth has fixed here.
#49 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Proof-Frontier Bootstrap (the lattice teaches the net for free) (wild:3) -- Every game's settled lattice contains thousands of variance-0 proof labels and proof-backed value targets that NO external dataset can give â€” distill the net on its own proven endpoints, closing the searchâ†’eval gap the lc0-same-net control exposed.
   MECH: After each think/game, harvest from the transposition DAG: (a) PROVEN nodes (mate/TB/ledger) as exact 0/1 WDL with variance 0; (b) interior nodes whose settled value is BACKED to within k plies of a proof (high-confidence soft labels weighted by proof-distance). Fine-tune BT4's value+WDL head (swtrain_venv, cu128 torch) with a proof-anchored loss: exact cross-entropy on variance-0 nodes, MSE-to-settled on proof-adjacent nodes, free-floating elsewhere. The teacher is the engine's OWN converged minimax-max lattice, not Stockfish â€” so it fixes exactly the positions our search proved but BT4 mis-valued (the 7.3cp candidate-set ceiling).
   HUGE: This is the only data source that is simultaneously free, in-distribution, perfectly labeled, and targeted at OUR failures â€” it turns the search's superiority over the raw net (8.3 vs 15.8cp) into permanent net weight, then re-searches with the better net for a compounding expert-iteration flywheel that needs no Stockfish and no billion-game corpus.
#50 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Distributional Eval Net (output the whole W/D/L belief, reason over its moments) (wild:4) -- Replace scalar value with a head that emits a full posterior over outcome AND a learned epistemic/aleatoric variance, so the lattice backs up DISTRIBUTIONS (and their higher moments) instead of means â€” the operation BT4's scalar value forbids.
   MECH: Train a head (on the proof-frontier corpus + human-game outcome variance) that outputs (W,D,L) PLUS a calibrated epistemic variance and skew per leaf â€” supervised against the empirical outcome distribution of near-identical lattice positions and against proof-collapse events (a node's distribution must collapse to its variance-0 proof). The lattice backs up the full distribution under minimax-max with variance propagation (the epistemic field is already built); selection and stopping read the propagated higher moments directly.
   HUGE: A mean hides the difference between a dead-drawn position and a double-edged 50/50 with identical EV â€” the distinction that decides whether to simplify or sharpen. A net that natively emits and a lattice that natively propagates the FULL distribution can play for variance (vs weaker opp) or against it (vs stronger) at the eval level, not via bolt-on contempt hacks â€” a strength axis lc0's scalar simply cannot represent.
#51 [WHAT STILLWATER UNIQUELY ENABLES â€” net] The Lattice as Working Memory (a net that reads neighbor beliefs, not just the board) (wild:4) -- Condition the evaluator on the ALREADY-COMPUTED beliefs of a position's lattice neighbors (parents, transposition-siblings, proven descendants), so the net amortizes the search's accumulated knowledge instead of judging each board cold.
   MECH: A small GNN/attention head ingests, for the query node, the settled (W,D,L)+epistemic+proof-status of its in-DAG neighbors (multi-parent predecessors, child frontier, nearest proven node + its proof-distance) and outputs a CORRECTION to BT4's raw value. Trained on harvest where the target is the final settled value and inputs are the mid-relaxation neighbor states â€” it learns the lattice's own backup operator as a contextual prior, so positions near a proof inherit its certainty before the relaxation even reaches them.
   HUGE: Every other engine re-judges each position from pixels; this net's input set GROWS with the persistent DAG, so a position evaluated near a banked theorem or a heavily-explored region is judged with that context baked in â€” eval quality rises with accumulated search, breaking the per-position-independence that caps all static nets.
#52 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Career Aquifer + Position Embedding Memory (a net that remembers what it learned about THIS structure) (wild:4) -- A learned position-embedding indexes a persistent cross-game memory of (structure â†’ corrected belief, proven theorems, opponent-specific outcomes); at eval time the net retrieves and conditions on its own past experience of similar positions.
   MECH: Train an embedding (contrastive: positions with same settled value/proof outcome are near) on the harvest + ledger; persist a per-career keyâ†’value store (Aquifer) of {embedding: settled belief, banked proofs, who-it-was-against, what-converted}. At inference, kNN-retrieve the top matches, feed their aggregated belief + variance as a conditioning vector to the value head. The ledger's variance-0 theorems are exact retrieval hits; soft memories are weighted by epistemic confidence and recency.
   HUGE: A frozen net plays its 10,000th game exactly like its first; this one accumulates a personal opening/structure book of CORRECTED evaluations and proven endgames that compounds over a career â€” the generalization-across-games asset lc0/SF structurally lack because they have no persistent memory between games.
#53 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Trajectory/Stability Head (predict the lattice's fate, kill the drift that costs ~31% of losses) (wild:5) -- A head that predicts not just a position's value but how that value will MOVE under further relaxation â€” directly attacking the diagnosed backup-DRIFT where a good move's value falls from its peak as search deepens.
   MECH: From harvest, label each node with the trajectory of its settled value across the node-ladder (768â†’4kâ†’16kâ†’40k) â€” a 'will-this-collapse-under-search' signal. Train a head to predict drift direction/magnitude + a stability score. The lattice uses it two ways: (1) damp backups from leaves predicted unstable (don't let a noisy deep frontier leaf masquerade as certain); (2) at the root, prefer moves whose winning value is predicted STABLE over those at an about-to-collapse peak. This is the hand-built VOI/epistemic head learned from real drift data instead of first-principles constants.
   HUGE: Drift to an inferior settled fixed point is ~31% of decisive losses and is INVISIBLE to scalar value (the search itself manufactures it). A net that forecasts the lattice's own convergence behavior fixes the failure mode that more search and constant-tuning provably cannot â€” the dominant non-eval bucket.
#54 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Proof-Pressure Field (a learned 'distance-to-decidability' that steers search toward winnable proofs) (wild:5) -- Train a head that predicts how FORCEABLE/PROVABLE a position is (its distance-to-a-theorem), turning the proof frontier into a dense gradient that pulls search and conversion toward lines that actually terminate in proven wins.
   MECH: Label harvest nodes by realized proof-distance (plies until they became PROVEN in the lattice, âˆž if never) and by whether a proof existed downstream. Train a 'proof-pressure' head predicting P(provable within H plies) + expected proof-distance. Use it as a selection/conversion bonus orthogonal to value: in saturated won endgames where BT4's value flatlines (the documented shuffle/no-gradient bug), proof-pressure provides the missing gradient toward the mating net; in the middlegame it biases toward forcing, decidable continuations.
   HUGE: It directly cures the value-saturation conversion failure (every winning move ~equal â†’ shuffle â†’ 3-fold) with a LEARNED progress signal, not hand-coded king-drive heuristics â€” and more deeply gives the search a sense of 'where certainty lives,' a wholly new objective beyond expected score.
#55 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Adversarial Proof-Refutation Net (a net trained to find the move the lattice's proof can't survive) (wild:5) -- A dedicated head/net trained specifically to PROPOSE the candidate that breaks a near-proof or stabilizes a fragile win â€” focusing the variance-0 machinery on exactly the moves whose resolution flips the verdict.
   MECH: Mine the lattice for 'fragile frontier' nodes: positions where the settled verdict is one refutation away from flipping (a proof depends on a single defended line, or a win rests on a deep unstable leaf). Train a policy-style head to rank moves by 'verdict-flip information' â€” supervised by which child, when proven, actually changed the parent's proof status in harvest. At inference this head selects the proof-burst targets and the defensive resources, so finite proof effort is spent where it converts beliefs to theorems fastest.
   HUGE: Proofs are the cheapest possible Elo (variance-0, can't hurt, ship on mechanism alone) but the prover currently fires on saturation heuristics; a net that aims the prover at flip-critical moves multiplies how many beliefs become theorems per GPU-second, expanding the variance-0 region of the game tree that nothing else can.
#56 [WHAT STILLWATER UNIQUELY ENABLES â€” net] Self-Calibrating Belief Net via Proof-Collapse Supervision (never-frozen, anchored, drift-proof) (wild:5) -- Continuously fine-tune the net online using the lattice's own proof collapses as ground-truth corrections â€” when a belief becomes a theorem, the gap between the prior prediction and the proven value is an EXACT, free, in-game training signal.
   MECH: Every time a node transitions beliefâ†’PROVEN during play (mate found, TB hit, ledger seed), record (net's earlier prediction at that node, exact proven outcome) as a variance-0 calibration pair. Run small test-time-training steps (swtrain_venv, between moves on banked clock â€” the clock the engine provably can't otherwise spend) that nudge weights to have predicted the now-known truth, regularized hard against the frozen base to prevent catastrophic forgetting. The proof anchors make online learning SAFE â€” you only ever train toward machine-checked facts.
   HUGE: Test-time training is normally dangerous (no ground truth at inference); here the proof frontier supplies a stream of perfectly-labeled corrections every game, so the net adapts to the live opponent/structure WITHOUT risk of training on noise â€” a never-frozen evaluator that lc0/SF cannot have because they generate no in-game variance-0 truth to anchor on.
#57 [NEURO-SYMBOLIC & LLM â€” inject the ling] Concept-Bottleneck Co-Eval (CBM-on-BT4) (wild:3) -- Insert a layer of NAMED, human-auditable chess concepts between BT4's trunk and a small learned value head, so the eval reasons in language even though BT4 supplies the perception.
   MECH: Distill ~200 LLM-named binary/scalar concepts (back-rank-weak, bad-bishop, outpost-secured, opposition-held, fortress-likely, king-safety-broken, passed-pawn-unstoppable) onto BT4's frozen trunk activations: LLM+Stockfish auto-label the billion-game corpus per concept, train linear probes (concept layer), then a tiny monotone head value = f(concepts). Lattice keeps BT4's NATIVE value as the readout (untouched, no detune) but ALSO carries the concept vector per node as a SECOND, decorrelated field used only to (a) widen the candidate set when a concept flips and (b) flag fortress/blockade so the search stops trusting BT4's saturated 0.7.
   HUGE: Turns the eval from an opaque scalar into a structured, decorrelated second opinion whose disagreements with BT4 are exactly the systematic blind spots (fortresses, conversion, long-term structure) that BT4's value cannot express; because it's an additive flag-channel not a value-rescale, it sidesteps the detune law that killed recalibration.
#58 [NEURO-SYMBOLIC & LLM â€” inject the ling] The Plan Oracle (LLM proposes, lattice disproves) (wild:4) -- An LLM reads the position like a coach and emits a typed PLAN with a falsifiable success-predicate; the lattice doesn't trust it â€” it PROVES or refutes the predicate and feeds only the surviving plan back as candidate-set + prior pressure.
   MECH: Per root, a small finetuned chess-LLM (trained on annotated-game prose: plans, prophylaxis, pawn-break theory) outputs 1-3 plans as {move-skeleton, target-square/structure, success-predicate} (e.g. 'minority attack: trade to leave Black an isolated c-pawn; success = Black has a fixed weakness on a half-open file within 12 plies'). The lattice treats each predicate as a goal: it injects the plan's moves into the candidate set with a temporary prior boost, runs its normal relaxation, and CHECKS the predicate against the settled DAG (structure is symbolically decidable). Plans whose predicate the lattice can't make progress on are dropped; surviving plans bias exploration toward strategically-coherent multi-move sequences BT4 policy ranks low.
   HUGE: BT4 policy sees one ply of priors and no multi-move INTENT; the documented losses are middlegame DRIFT (good move found then abandoned) â€” a verified plan gives the search a persistent symbolic GOAL to organize many plies around, exactly the long-horizon coherence a per-ply value/policy cannot hold. The LLM injects strategic knowledge from literature the self-play net never read.
#59 [NEURO-SYMBOLIC & LLM â€” inject the ling] Proof-Reachability Head (predict what STILLWATER can PROVE) (wild:4) -- Train a head that predicts the probability the lattice could PROVE a win/draw within N plies, turning STILLWATER's own variance-0 proof events into a self-supervised symbolic target that guides where to spend calculation.
   MECH: Mine the proof ledger + distillery: every position the lattice ever proved (mate, TB-pin, fortress) is a positive; near-misses are negatives. Train a small head on BT4 trunk activations to output P(provable-win | depth budget) and expected proof-distance. At search time this is a THIRD field: nodes with high proof-reachability but unproven get calculation priority (concentrate to convert), and high-reachable-DRAW nodes downweight BT4's optimistic win-value (anti-drift, anti-fortress-overvalue). Self-improving: every new game adds proof events, retrains the head â€” career memory.
   HUGE: STILLWATER uniquely manufactures variance-0 ground truth (proofs) the way nothing else does; this converts that exhaust into a learned 'where is certainty cheap' signal that directs finite calculation toward closable positions â€” directly attacking the conversion/drift losses that are ~60% of the deficit, using the engine's OWN epistemics rather than borrowed values.
#60 [NEURO-SYMBOLIC & LLM â€” inject the ling] Symbolic-Regression Residual (rediscover Steinitz, then exceed him) (wild:3) -- Use program synthesis to fit a tiny, INTERPRETABLE symbolic formula to the residual where BT4 is systematically wrong â€” yielding human-readable correction terms that can be audited and that generalize off-distribution because they're laws, not lookups.
   MECH: From the SF-distillation corpus, take the residual (SF_value âˆ’ BT4_value), and run symbolic regression (PySR-style genetic program search over a chess-feature grammar: material imbalances, king-ring counts, pawn-chain predicates, piece-coordination) to find compact formulas explaining the residual. Ship ONLY terms that (a) are human-meaningful and (b) improve held-out MAE AND survive a timed gauntlet as an ADDITIVE, search-aware term â€” applied at the candidate-WIDENING stage, never as a value rescale (which detunes). The LLM seeds/names the grammar from endgame and positional theory.
   HUGE: A symbolic law generalizes to positions outside any training distribution (the whole point of principles), and being interpretable, each term can be individually gauntlet-gated so it can only help â€” turning the failed value-recal into a SAFE, auditable patch set that targets BT4's known structural blind spots (fortress, opposite-bishops, conversion) with the orthogonality a monolithic rescale lacked.
#61 [NEURO-SYMBOLIC & LLM â€” inject the ling] Linguistic Retrieval-Conditioned Eval (annotate-then-evaluate) (wild:3) -- Retrieve human ANNOTATIONS for similar positions and let an LLM compress the prose into a structured strategic-context vector that conditions the eval â€” injecting collective human judgment about position-TYPES the net averaged away.
   MECH: Build a position->annotation index from millions of annotated games (the local corpora are full of commented PGNs). At eval time, kNN-retrieve the nearest annotated positions via BT4 embeddings; an LLM reads their human comments and emits a compact structured context ('this is a Carlsbad structure; plan = minority attack; key risk = e4 break; typical result = White edge'). That context vector conditions BT4's value head (small FiLM adapter) and, crucially, sets the lattice's DRAW-contempt and stopping rule per position-type (sharp-theory vs fortress-prone). It's the embeddings-kNN direction but the retrieved payload is HUMAN PROSE distilled by an LLM, not raw values.
   HUGE: Self-play nets never saw a word of explanation and average over position-types that humans treat as categorically different; conditioning on the retrieved STRATEGIC TYPE injects an orthogonal, literature-grounded prior that resolves exactly the type-confusions (fortress vs win, sharp vs quiet) where a single scalar value fails.
#62 [NEURO-SYMBOLIC & LLM â€” inject the ling] The Refutation Dialogue (self-play in LANGUAGE, not moves) (wild:5) -- Two LLM agents argue the position ('White's attack crashes through' vs 'the defense holds via ...Rf8 and the king walks'); each claim becomes a concrete line the lattice is forced to resolve, and who-was-right trains a verbal-judgment head.
   MECH: An Attacker-LLM and Defender-LLM, finetuned on annotated games, generate competing natural-language assessments + the concrete moves backing each. Every asserted line is handed to the lattice as a forced calculation target (proof or settled value). The lattice adjudicates the debate; the winning side's reasoning + the position embed becomes a training pair for a 'verbal-verdict' head, and the surviving lines enter the candidate set. Over a career, the head learns which kinds of human verbal arguments the calculator confirms â€” a debate-distilled strategic prior.
   HUGE: Adversarial language generation systematically SURFACES the critical defensive/attacking resources a one-shot policy misses (the documented tail blunders are missed single replies), and grounding every verbal claim in machine proof means the LLM's hallucinations are filtered â€” you keep only literature-knowledge that calculation confirms, accumulating it across the career.
#63 [NEURO-SYMBOLIC & LLM â€” inject the ling] Compositional Eval Graph (the eval becomes a typed PROGRAM) (wild:5) -- Replace the scalar value with a small LLM-synthesized PROGRAM â€” a typed graph of named sub-evaluators (king-safety, structure, activity, endgame-class) whose composition rule and weights the lattice can introspect, edit, and PROVE-override.
   MECH: An LLM writes (once, offline) an evaluation PROGRAM: a DAG of differentiable named modules, each a tiny head on BT4 features, combined by an explicit, branch-on-phase composition the LLM authored from theory ('if opposite-colored bishops and no other pieces, route through fortress-detector and cap value'). Modules trained jointly on the corpus; the COMPOSITION is symbolic and editable. The lattice can override any subterm with a proof (TB/fortress) and can attribute a verdict to specific modules â€” so a wrong eval is debuggable to a named cause and patchable without retraining the whole net.
   HUGE: A monolithic scalar can't be locally corrected (every recal detuned globally); a compositional typed program lets PROOFS and symbolic laws override individual subterms with surgical, non-detuning precision, and the phase-branching encodes the categorical position-type knowledge BT4 smears â€” finally giving the engine an eval whose structure matches how chess knowledge is actually organized.
#64 [NEURO-SYMBOLIC & LLM â€” inject the ling] Theorem-Schema Memory (named motifs as transferable proof templates) (wild:4) -- An LLM names the MOTIF behind every proof the engine finds (Lucena, Philidor, smothered mate, Greek-gift, fortress-of-type-X) so future positions matching the schema get the result by ANALOGY before any GPU eval â€” symbolic generalization of the proof ledger.
   MECH: Each ledger theorem is abstracted by an LLM into a named, parameterized schema with an applicability predicate ('Lucena: rook-pawn-vs-rook with attacker king ahead, bridge-buildable'). New positions are matched against the schema library by a learned classifier on BT4 embeddings + symbolic predicate check; a match injects the schema's KNOWN technique (move sequence / value pin) as a high-prior candidate and a near-proof, which the lattice then verifies cheaply. The library grows across the career; the LLM keeps inventing names for recurrent unnamed patterns the engine proves.
   HUGE: Today every endgame is re-solved from scratch (proof ledger only re-fires on the EXACT position); schema-level transfer lets a single proved instance generalize to an infinite family the way a human who learns Lucena once knows all of them â€” a categorical jump from position-memory to PRINCIPLE-memory, compounding without bound over a career.
#65 [DELIBERATELY ABSURD / LATERAL â€” ideas ] The Liar's Oracle (Adversarial Eval Twin) (wild:5) -- Train a SECOND net whose only job is to LIE convincingly, then make the truth-net stronger by learning to detect its own lies.
   MECH: Fine-tune a small 'Liar' head that perturbs BT4's WDL toward the most plausible-but-wrong value (max-likelihood under the policy, max-error under Syzygy/proof ground truth). At inference, the difference (BT4 logits minus where-the-Liar-would-pull-them) is a self-consistency confidence signal: positions where BT4 and the Liar nearly agree are exactly the positions BT4 evaluates by shallow pattern (deceivable), so the engine routes extra search/proof-bursts there. Train both adversarially on the harvest corpus with proofs as the incorruptible referee.
   HUGE: It manufactures a free per-position 'is BT4 about to be fooled here?' detector WITHOUT a stronger teacher â€” the one thing a frozen evaluator can't give you â€” turning the eval ceiling from a wall into a map of its own holes that search can patch exactly where it matters.
#66 [DELIBERATELY ABSURD / LATERAL â€” ideas ] Tablebase Hallucination (Dreaming Backward From Won Endings) (wild:4) -- Run the engine BACKWARD from every 7-man Syzygy win, hallucinating the middlegames that could have led there, and train BT4 on those dream-positions labeled with PERFECT value.
   MECH: Use retrograde move generation from Syzygy-won positions to grow a tree of legal predecessors 20-40 plies up into late-middlegame territory; each such position inherits a variance-0 label via the proven distance-to-mate of its best continuation (game-theoretic value backed up through the retro-tree, not estimated). This yields a MASSIVE perfectly-labeled corpus exactly in the conversion regime where the memory says BT4 under-values won positions (raw_v~0.7). Fine-tune on it; the labels are not a stronger engine's guesses, they are GROUND TRUTH.
   HUGE: It is the only known way to get perfect (variance-0) training labels for non-trivial middlegame-ish positions without a stronger oracle â€” it converts the 3-7man tablebases from a 7-piece endgame crutch into a deep, perfect curriculum for the exact eval weakness (conversion) that detunes everything else.
#67 [DELIBERATELY ABSURD / LATERAL â€” ideas ] The Bored Engine (Surprise-Minimizing Evaluator) (wild:4) -- Reward the evaluator for being BORED â€” minimize how often the settled lattice verdict surprises the raw net â€” and let chronic surprise auto-locate the positions where the net is structurally wrong.
   MECH: Already in the Distillery: net first-impression vs settled verdict. Define per-position 'surprise' = KL(settled-WDL || raw-WDL). Continuously fine-tune BT4 (cloud GPU, never-frozen) with a free-energy objective: predict the SETTLED distribution, weighting by surprise so the net's gradient flows hardest exactly where its snap judgment was most wrong. The lattice becomes a self-generating teacher that is strictly >= the raw net (search adds information), so distillation has positive signal even without Stockfish.
   HUGE: It is a bootstrapping flywheel: the search makes the eval better, the better eval makes the search settle faster/deeper, repeat â€” a within-our-own-system AlphaZero loop that needs NO external teacher and is immune to the 'value-recal detunes search' trap because it distills the SEARCH's own output, the thing the search is already tuned to.
#68 [DELIBERATELY ABSURD / LATERAL â€” ideas ] Chess Played As Thermodynamics (Belief-Field Relaxation Net) (wild:5) -- Stop predicting a position's value; predict the whole BOARD's local belief field and let a learned PDE relax it to equilibrium like heat diffusing.
   MECH: Replace the scalar/WDL head with a net that emits a per-square 'tension field' (attack/defense/mobility potentials). A learned graph-diffusion operator (GNN message-passing as the lattice backup) relaxes this field across the transposition DAG until it reaches a fixed point; the global value is read out from the equilibrium field, not regressed directly. Train the diffusion operator so its fixed point matches settled lattice verdicts and proven results.
   HUGE: Scalar eval throws away WHERE the imbalance is; a relaxing field is naturally compositional and transposition-aware (the same field equilibrates over the DAG STILLWATER already maintains), potentially generalizing far better from few labels because physics-style locality is a massive inductive prior.
#69 [DELIBERATELY ABSURD / LATERAL â€” ideas ] The Engine That Plays To Be Understood (Provability-Maximizing Eval) (wild:4) -- Bias the evaluator to prefer moves that lead to PROVABLE positions, treating mathematical certainty itself as a component of value.
   MECH: Add a 'crystallizability' term to the eval target: positions from which proof-bursts and Syzygy frequently CONVERT belief into variance-0 theorems get a small value bonus, learned from the harvest record of which positions later became proven. The net learns to steer the game toward regions where STILLWATER's unique proof machinery dominates â€” heavy-piece simplifications, forced sequences, tablebase basins â€” turning the proof engine from a passive checker into an active strategic preference.
   HUGE: It weaponizes STILLWATER's ONE structural advantage over SF/lc0 (machine-checked proofs as variance-0 anchors) into a playing STYLE: steer toward where you are objectively, provably better-equipped than a heuristic opponent, instead of fighting on BT4's eval-quality turf where you're a hair behind.
#70 [DELIBERATELY ABSURD / LATERAL â€” ideas ] Reincarnation Net (Career-Long Weight Drift With Opponent Memory) (wild:4) -- The net is NEVER the same twice â€” it carries a slowly-drifting 'mood' vector conditioned on every game it has ever played, like a player who ages.
   MECH: Maintain a low-rank LoRA-style adapter on BT4 that is updated by a tiny gradient step after every single game (test-time training), conditioned on a persistent embedding of the opponent (the Effigy/rho) and the Aquifer cross-game lattice. The adapter encodes 'what has been working lately' and 'who I am playing'; it is checkpointed as a career. Anchored against drift by replaying proof/Syzygy variance-0 positions each step so it can never forget hard truths.
   HUGE: A frozen net is a player with permanent amnesia; a net that integrates its entire game history can develop opening preparation, exploit recurring opponents, and accumulate practical wisdom across a career â€” compounding strength that a static evaluator categorically cannot reach.
#71 [DELIBERATELY ABSURD / LATERAL â€” ideas ] The Hivemind Vote (Ensemble of Deliberately Insane Nets) (wild:5) -- Run BT4 alongside several CHEAP nets each trained to be brilliant in one narrow regime and clinically insane everywhere else, and let the lattice's belief-variance pick whom to trust per position.
   MECH: Train tiny specialist heads (a king-hunt fanatic, a fortress-paranoid drawkiller, a pawn-endgame savant) each overfit HARD to one phase/motif from the corpus. At a node, weight their WDL contributions by a learned gating that reads the epistemic-uncertainty field STILLWATER already computes â€” high BT4 epistemic -> defer to whichever specialist is confident AND in-domain. The lattice fuses them as distributions, not scalars.
   HUGE: BT4 is a generalist near its ceiling everywhere; a mixture-of-deranged-experts can EXCEED it locally in the exact narrow regimes (conversion, fortresses, sharp king attacks) where the generalist is provably weakest, with near-zero added cost because the specialists are tiny and gated.
#72 [DELIBERATELY ABSURD / LATERAL â€” ideas ] Eval By Self-Play Against A Mirror Of The Opponent's Mistakes (wild:4) -- Don't evaluate the position objectively â€” evaluate it as 'how much rope does THIS opponent's specific blunder-fingerprint give me here', learned from a billion human games.
   MECH: Embed each position and retrieve, from the Lichess billion-game corpus, the empirical human-move distribution AND the conditional 'human error magnitude' at that position (how often humans/this-rating-band go wrong here). Train a second eval head predicting OBJECTIVE_value + practical_winnability = E[outcome | opponent samples from their real error distribution]. The Effigy's rho conditions the blend; the kNN over a position-embedding memory makes it position-specific, not a global contempt scalar.
   HUGE: Against any non-perfect opponent (every opponent), maximizing PRACTICAL win probability â€” steering into positions where THIS opponent reliably errs â€” beats maximizing objective eval, and a billion-game empirical error map is a strength source that has NOTHING to do with the BT4 eval ceiling, sidestepping it entirely.
#73 [INFORMATION-THEORETIC / FIRST PRINCIPLES] The Game-Tree-Complexity Thermometer (eval as a learned sufficiency gauge) (wild:4) -- Don't predict the value; predict whether the position's value is yet KNOWABLE from board features, and route compute by that.
   MECH: Train a second head on the BT4 embedding to predict the *irreducible residual* between BT4's WDL and Stockfish-d40 WDL across the Lichess-annotated billion-game corpus â€” i.e. a calibrated map of WHERE on the board-feature manifold BT4 is provably wrong. At inference this 'sufficiency gauge' is the minimal-sufficient-statistic detector: positions it flags as under-determined (board features insufficient â†’ true value depends on deep tactics) get a variance-INFLATED belief that forces the lattice to search them, while flagged-sufficient positions snap. The gauge IS the missing decomposition of eval error into 'aleatoric-by-position' vs 'fixable-by-search'.
   HUGE: It directly attacks the slack's LOCATION rather than its magnitude: BT4's 200-Elo gap to perfect play is concentrated in a thin set of positions where board statistics are an insufficient statistic, and a calibrated detector of exactly that set lets every search second be spent where eval is actually blind â€” converting STILLWATER's distribution-native variance into a principled compute allocator no value-recalibration can give.
#74 [INFORMATION-THEORETIC / FIRST PRINCIPLES] Holographic value via group-averaged eval (the full chess symmetry group, not just the 8 board symmetries) (wild:4) -- Make eval EXACTLY invariant to every value-preserving transformation by averaging BT4 over the orbit, turning thousands of correlated reads into one ultra-low-variance estimate.
   MECH: Beyond the 8 dihedral board symmetries, chess value is invariant under a far larger learned equivalence: color-swap+side-to-move flip (exact), and APPROXIMATELY under tempo-neutral piece relabelings and material-translation symmetries discoverable by mining positions BT4/Stockfish rate identically. At each node, evaluate BT4 on a sampled orbit of these transforms and average in WDL-logit space; the variance of the orbit reveals where BT4 VIOLATES a true invariance (a pure eval bug) and the mean is a Rao-Blackwellized, lower-variance sufficient statistic. Exact symmetries are free signal; near-symmetries are a regularizer that cancels BT4's idiosyncratic noise.
   HUGE: Rao-Blackwellization over the true symmetry group is a guaranteed variance reduction with ZERO new training â€” it extracts more bits per position from the SAME net, and orbit-disagreement is a free, label-free detector of eval error exactly where the net is non-physical.
#75 [INFORMATION-THEORETIC / FIRST PRINCIPLES] Eval as the fixed point of a Bellman operator solved at inference (STILLWATER already IS the solver) (wild:3) -- Stop reading BT4's value; read only its POLICY+terminals and let the lattice relaxation BE the value function â€” value computed, never predicted.
   MECH: Reframe the lattice not as backing-up BT4 values but as a learned-operator fixed-point solver: pin only variance-0 anchors (Syzygy, proofs, mates) as boundary conditions, take BT4 strictly as the policy/transition model (which the gap-attribution found is near-perfect: SF-best in top-5 93%), and define value purely as the unique fixed point of the discounted backup over the transposition DAG seeded from those anchors. BT4's value enters only as a leaf PRIOR with explicit, decaying weight that vanishes as a node's subtree resolves. The mathematically-principled construction: value = solution of a contraction mapping anchored on PERFECT information, not a regression target.
   HUGE: It removes BT4's value error from the asymptote entirely â€” at full resolution the answer is the true minimax value relative to perfect boundary anchors, with BT4 only choosing which leaves to expand; this is the categorical move from 'predict the answer' to 'compute the answer', the same jump alpha-betaâ†’ground-truth that nets blurred.
#76 [INFORMATION-THEORETIC / FIRST PRINCIPLES] The compression-optimal evaluator (best arithmetic coder of master games = best player) (wild:4) -- Train the eval to MINIMIZE the bits needed to encode the next move in real games, making P(move) literally the optimal predictive distribution â€” Solomonoff for chess.
   MECH: On the billion-game Lichess+engine corpus, fine-tune a policy/value model with a pure description-length objective: minimize cross-entropy (= code length) of the actual continuation conditioned on the position, weighted by the players' strength. By the coding theorem the model that compresses strong-player continuations best is the one whose conditional distribution is closest to the true value-induced optimal policy. STILLWATER reads this as the broker prior; the compression LOSS itself is a per-position 'surprise' signal that flags positions where even the best compressor is uncertain â€” the information-theoretic frontier.
   HUGE: It targets the provably-correct objective (predictive code length) instead of a hand-chosen value/policy loss, so the ceiling is the entropy of optimal play itself; and bits-of-surprise is a principled, dimensionless search-allocation signal with no free constants.
#77 [INFORMATION-THEORETIC / FIRST PRINCIPLES] The potential field whose gradient is the move (eval as a scalar field, move = steepest ascent) (wild:5) -- Learn a single smooth scalar potential Phi over position-space whose discrete gradient across legal moves directly yields play, decoupling 'which move' from 'how good'.
   MECH: Define Phi(s) as a learned potential and require the move-ranking to be exactly the finite-difference gradient: rank(m) = Phi(child) âˆ’ Phi(s). Train Phi by a Helmholtz-style decomposition of the Stockfish-annotated corpus into a CONSERVATIVE part (path-independent positional potential â€” the thing that should transpose perfectly) and a NON-conservative residual (tactical, path-dependent). STILLWATER's transposition DAG makes the conservative/curl split observable: any value that fails to be path-independent across transpositions is provably the tactical residual. Read Phi for quiet eval, route the curl-residual positions to search.
   HUGE: It separates eval into a provably-separable conservative field (perfectly transposition-consistent, the part a static net SHOULD nail) and a curl that is exactly the irreducible search component â€” a principled decomposition that tells you, per position, how much of the value is even static-learnable vs must-be-searched.
#78 [INFORMATION-THEORETIC / FIRST PRINCIPLES] Tablebase-anchored value bootstrapping up the man-count ladder (perfect labels propagated by interpolation) (wild:3) -- Use the 3-7-man Syzygy tablebases as variance-0 ground truth and learn the eval purely as the minimal smooth extrapolation that is EXACT on every tablebase position.
   MECH: Treat perfect tablebase WDL/DTZ as the only trusted labels and fit the eval as the lowest-complexity function that exactly reproduces them, then bootstrap one man at a time: an 8-man eval is trained so that after one capture/promotion it lands on a 7-man position whose value is KNOWN-exact, i.e. consistency against a perfect boundary. STILLWATER's lattice supplies the propagation (every node within k captures of a TB position gets a partially-pinned belief); training minimizes eval inconsistency with the perfect lower-man layer reachable beneath it. The corpus is INFINITE and PERFECT (generate any TB position), sidestepping the 'local corpus too small' wall.
   HUGE: It replaces noisy game-outcome labels with literally perfect ones and turns eval-learning into a boundary-anchored extrapolation problem with an unbounded clean dataset â€” the only training signal in chess with zero label noise, and the man-count ladder gives a principled curriculum toward the full game.
#79 [INFORMATION-THEORETIC / FIRST PRINCIPLES] The minimal-state critic: distill eval onto the provably-sufficient sub-statistic (wild:5) -- Discover the smallest set of position features that is information-sufficient for value via an information bottleneck, then evaluate ONLY that â€” exposing exactly where the board (alone) is insufficient.
   MECH: Train a stochastic bottleneck z = f(board) under an information-bottleneck objective: maximize I(z; Stockfish-value) while minimizing I(z; board), over the annotated corpus. The converged z is an empirical approximation to the minimal sufficient statistic of a chess position for value. Two payoffs: (1) eval through the tiny sufficient z is maximally noise-robust and generalizes far harder than a full-board net (it has thrown away everything value-irrelevant); (2) positions where ANY lossless-for-value z must retain near-full board information are precisely the tactically-sharp positions where no static statistic suffices â€” a derived, not heuristic, definition of 'needs search'.
   HUGE: It answers the lens's literal question â€” what is the minimal sufficient statistic for perfect play â€” and a critic built on it should generalize categorically better than a parameter-heavy board net (less to overfit, the right invariances baked in by construction), while simultaneously delineating the searchable frontier.
#80 [INFORMATION-THEORETIC / FIRST PRINCIPLES] Decorrelated dual-evaluator with provable error-cancellation (information addition, not recalibration) (wild:3) -- Combine BT4 with a SECOND eval trained to be MAXIMALLY DECORRELATED in its errors, so their fusion has strictly lower variance than either â€” adding bits the project never had.
   MECH: The memory's own decomposition says ~19% of decisive losses are net-blind and a shallow decorrelated SF-d10 ranks the right move in 11/13 of those. Train/obtain a second evaluator (e.g. a searchless GM-transformer or a tablebase-anchored critic) under an explicit objective that PENALIZES error-correlation with BT4 on the annotated corpus, then fuse in WDL-logit space with weights set by their measured error-covariance (BLUE/optimal-linear-unbiased estimator). The fusion variance is bounded below by the off-diagonal covariance â€” minimizing correlation is the whole game. STILLWATER reads the fused belief; fusion DISAGREEMENT is itself a high-value search trigger.
   HUGE: Value-recalibration of BT4 detuned the search (a monotone transform adds NO bits); a decorrelated second eval adds genuinely independent information, and the BLUE fusion is the provably-minimum-variance estimator from two sources â€” the only path that raises the eval CEILING rather than reshuffling BT4's own bits.
#81 [CROSS-DOMAIN TRANSFER â€” importing brea] Discrete-Diffusion Plan Denoiser (eval over whole continuations, not positions) (wild:4) -- Borrow text/image discrete-diffusion to GENERATE a full plausible 12-ply continuation in one shot, then evaluate the position by how its denoised futures resolve â€” a learned 'imagination rollout' the frozen BT4 cannot give.
   MECH: Train a discrete-diffusion transformer (absorbing-state, D3PM-style) on billions of Lichess/SF-annotated games to denoise a masked move-sequence conditioned on the current FEN into a coherent multi-ply continuation; at eval time sample K denoised continuations per leaf and feed their TERMINAL BT4-WDL back as a second, trajectory-grounded belief that STILLWATER pins alongside the native leaf value (never replacing it). Training is pure imitation (no self-play), so the existing game corpus suffices.
   HUGE: It supplies the one thing a position-only evaluator structurally lacks â€” a calibrated guess of WHERE the game is going â€” fixing the documented backup-DRIFT (~31% of losses) and conversion failures at their root, because a leaf is now scored by its denoised futures, not a single saturated WDL. Because it ADDS a decorrelated channel rather than rewriting the readout, it dodges the 5-for-5 reorganizer curse.
#82 [CROSS-DOMAIN TRANSFER â€” importing brea] MuZero-Inverted: Latent DYNAMICS welded onto the frozen BT4 (plan in transposition-space) (wild:5) -- Keep BT4 as the value/policy head but learn a MuZero-style latent transition model so the lattice relaxes in an abstract space where transpositions and long-range plans are GEOMETRICALLY native, not Zobrist-keyed coincidences.
   MECH: Distill a learned dynamics function g(z, move)->z' and an embed h(FEN)->z trained so that BT4's value/WDL is a linear readout of z (value-equivalent, MuZero-Reanalyze on the existing corpus â€” no self-play needed). STILLWATER's belief lattice then keys nodes by latent z (cosine-bucketed) instead of/alongside Zobrist, so positions that are STRATEGICALLY identical but not bit-identical share belief mass â€” a generalization a hash table cannot express.
   HUGE: Transposition reuse today is exact-key only (memory shows 1.0-2.8x reuse, pure bit-coincidence); latent keying makes 'same plan, different move order, slightly different pawn' a single node â€” the lattice would propagate one hard-won evaluation across a whole equivalence class, multiplying effective search depth without more evals (the eval-bound regime's only escape).
#83 [CROSS-DOMAIN TRANSFER â€” importing brea] Chess-as-Proving: an AlphaProof-style tactical PROVER that emits Syzygy-grade variance-0 anchors mid-board (wild:4) -- Transplant neural theorem proving (AlphaProof/AlphaGeometry expert-iteration over a formal verifier) to turn STILLWATER's machine-checked proof machinery into a learned mate/zugzwang prover that mints variance-0 pins deep in the MIDDLEGAME, not just endgames.
   MECH: Treat 'this position is a forced win in <=N' as a theorem; a policy network proposes candidate forcing trees, the existing sound prover (core-rs/prover.rs, checks-only attacker) is the VERIFIER, and expert-iteration trains the proposer on its own found proofs (AlphaProof's exact loop) plus the SF-annotated corpus for warm start. Verified theorems enter the lattice as variance-0 anchors that proof-propagate, exactly like tablebase hits.
   HUGE: STILLWATER uniquely treats proofs as variance-0 beliefs that dominate everything â€” but today they only fire near mate/TB. A learned prover extends ABSOLUTE CERTAINTY (not probabilistic WDL) tens of plies earlier, collapsing whole subtrees the way a 7-man TB hit does; certainty is immune to the drift/saturation that causes 80% of search-side losses.
#84 [CROSS-DOMAIN TRANSFER â€” importing brea] RLHF for the BACKUP operator: a learned reward model that catches DRIFT before it's played (wild:3) -- Borrow RLHF's reward-model-from-preferences to train a discriminator on PAIRS of STILLWATER trajectories (its settled verdict vs the move SF/lc0 actually preferred) that flags when the lattice is about to settle on an inferior fixed point.
   MECH: Mine the Distillery harvest + SF/lc0 disagreements into preference pairs (settled-pick LOST-to / preferred-move), train a small reward model r(position, candidate-move, lattice-state-summary)->preference; at decision time it acts as a veto/uncertainty gate (NOT a value override) that re-opens search on flagged moves. Pure offline preference learning on data we already produce.
   HUGE: The memory's #1 unsolved loss mode is backup-DRIFT: more search DEVALUES the good move and settles wrong (~31% of decisive losses, the engine can't self-detect it). RLHF reward models excel at exactly 'this looks subtly worse to a stronger judge' â€” importing them gives STILLWATER an external conscience for its own settling, addressing the structural failure tuning never fixed.
#85 [CROSS-DOMAIN TRANSFER â€” importing brea] Foundation-model transfer: fine-tune a 7B code/math LLM into the evaluator (escape the from-scratch-corpus wall) (wild:4) -- Exploit scaling-law transfer â€” a large pretrained reasoning LLM already contains chess from pretraining â€” by LoRA-fine-tuning it on the SF/Lichess corpus into a WDL+best-move head, breaking the 'corpus 3-4 orders too small to train from scratch' wall via borrowed priors.
   MECH: Take an open reasoning LLM, attach value/policy heads, fine-tune with LoRA on PGN+FEN+SF-WDL labels (the corpus is plenty for fine-tuning, not pretraining); run as a SECOND, slow, decorrelated oracle consulted only at the root and at lattice nodes the reward-gate flags â€” its output pins as an extra belief, never the hot-loop eval.
   HUGE: BT4 is the objective net ceiling we BORROWED and cannot exceed by retraining it; a foundation model is a DIFFERENT function class with emergent abilities (it can read a position's strategic motif in language-space). The whole point of scaling-law transfer is that pretraining priors substitute for the data you don't have â€” exactly STILLWATER's blocker.
#86 [CROSS-DOMAIN TRANSFER â€” importing brea] In-Context RL career memory: an opponent/position 'episode tape' the net reads to adapt without weight updates (wild:5) -- Import in-context RL (Algorithm Distillation / DPT) â€” a model that improves purely from a context buffer of past episodes â€” so STILLWATER's persistent DAG becomes a read-tape that adapts evaluation to THIS opponent and THIS structure across a career, with frozen weights.
   MECH: Train a transformer to take a context = sequence of (position, played-move, eventual-outcome) tuples retrieved from the persistent lattice/ledger/opponent-history and emit a CONDITIONED value/policy correction; at inference the relevant career episodes are retrieved (kNN over embeddings) and prepended, so the eval is a function of accumulated experience, in-context, no gradient step.
   HUGE: STILLWATER already has the substrate nothing else does â€” a persistent cross-game DAG, proof ledger, and Effigy opponent model â€” but the EVALUATOR ignores all of it. In-context RL is the mechanism to make a frozen net LEARN from that career tape at test time, turning memory into measurable strength and a genuinely self-improving player over thousands of games.
#87 [CROSS-DOMAIN TRANSFER â€” importing brea] Program-Synthesis of plan-predicates: the engine WRITES verifiable strategic features for itself (wild:4) -- Borrow DreamCoder/library-learning program synthesis to induce a growing library of human-readable, machine-CHECKABLE positional predicates (e.g. 'opposite-side castling race', 'minority attack live') that decorrelate from BT4's opaque value.
   MECH: An LLM/enumerative synthesizer proposes candidate board-predicate programs over a chess DSL; each is empirically scored for marginal predictive power over BT4-WDL on the SF corpus (does conditioning on it reduce residual?), survivors enter a learned library (DreamCoder wake-sleep), and the lattice gets a sparse, interpretable feature vector that gates a tiny corrector â€” but unlike the dead ridge corrector, the features are SEARCHED-FOR to be decorrelated, not 8 hand-picked summaries.
   HUGE: The ridge/recal correctors failed because their features were redundant with raw_v (monotonic, no new info). Program synthesis explicitly OPTIMIZES for orthogonal signal and yields verifiable, debuggable predicates â€” a path to add real eval information that the value-recal dead-end proved you cannot get by rescaling the existing one.
#88 [CROSS-DOMAIN TRANSFER â€” importing brea] Sparse-MoE Mixture-of-Specialists oracle routed by phase/structure (capacity without throughput cost) (wild:3) -- Transplant extreme-scale sparse MoE â€” total capacity huge, active params tiny â€” by training many small EXPERT correction-heads (opposite-castling, fortress, opposite-bishop, K+P, sharp-tactical) and routing each leaf to the 1-2 relevant experts atop frozen BT4.
   MECH: Cluster the SF-annotated corpus into structural regimes (learned router on the BT4 embedding), train a tiny specialist value/policy-correction head per cluster, and at eval a top-1 gate fires only the matching expert (a few hundred K params) onto BT4's frozen trunk â€” total knowledge scales with #experts while per-eval cost stays near-BT4, preserving the throughput the engine is bound by.
   HUGE: BT4 is one dense net that must be mediocre-everywhere; the documented failures cluster by PHASE (drift in middlegame, saturation/shuffle in won endgames). A fortress-specialist or a won-endgame-specialist that only fires in-regime can fix the exact pockets a single dense net averages away â€” and MoE is the only known way to add that capacity without paying it on every eval in an eval-throughput-bound engine.
#89 [THE EVALUATOR'S BLIND SPOTS â€” attack B] DISTANCE-FIELD HEAD (mate-potential, not win-probability) (wild:3) -- Replace the saturating WDL value with a learned signed distance-to-conversion field that keeps a gradient inside already-won positions.
   MECH: Add a head trained on Syzygy DTZ/DTM (variance-0 ground truth for <=7-man) extended to the full board by regressing expected plies-of-progress to a tablebase-won frontier from Stockfish-annotated Lichess games; the lattice backs up min-distance like a proof instead of mean-WDL, so every winning move differs in score and the engine always sees downhill toward mate.
   HUGE: Directly kills the #1 measured bug: the ~0.7 WDL plateau where every winning move reads equal so the engine shuffles into a 3-fold. The field is monotone in progress, so won positions get a true gradient. This is the conversion ceiling that DrawContempt and king-drive readouts only patch around.
#90 [THE EVALUATOR'S BLIND SPOTS â€” attack B] MAX-BACKUP-NATIVE EVALUATOR (train the net for the operator that reads it) (wild:4) -- Re-train BT4's value head so its outputs are self-consistent under the lattice's minimax-MAX backup, not under MCTS visit-mean.
   MECH: Generate targets by running STILLWATER's own max-backup relaxation to a deep fixed point on millions of harvested positions, then fine-tune the value head (cu128 GPU) with a fixed-point/TD-consistency loss v(s)=max_a v(s') under OUR backup, closing the loop the memory names: BT4 was trained to be read by visit-counts, we read it against the grain.
   HUGE: lc0 extracts ~3600 from the SAME net we get 3432 from because we read a visit-mean-calibrated net with a max operator, drifting to inferior fixed points; an evaluator whose training operator IS our backup makes the lattice's fixed point the CORRECT one, recovering most of the ~170-Elo extraction gap without changing search.
#91 [THE EVALUATOR'S BLIND SPOTS â€” attack B] EVIDENTIAL EVALUATOR (the net outputs its own error bars) (wild:3) -- A net that emits epistemic plus aleatoric uncertainty per position so the lattice trusts max-backup only where the eval is reliable.
   MECH: Add a Normal-Inverse-Gamma / Dirichlet-evidential head trained with a calibration loss against held-out deep-Stockfish labels (exact 0 variance on Syzygy/proofs); the lattice's evidence-gated max (LCB) already exists but consumes a hand-built proxy, so feed it the NET'S epistemic variance instead, averaging where the net is unsure and maxing where it's confident.
   HUGE: Every search-reorganizer (VOI, honest-max, visit-readout) died 5-for-5 because their uncertainty was hand-crafted and wrong; a LEARNED calibrated per-position epistemic signal is the missing input that could finally make robust backup work, telling the lattice exactly where the drift-prone middlegame fixed points are untrustworthy.
#92 [THE EVALUATOR'S BLIND SPOTS â€” attack B] FAILURE-MINED FINE-TUNE (train on exactly where BT4 is wrong) (wild:2) -- Build the training corpus to be ONLY the positions where BT4's value disagrees with deep Stockfish/Syzygy truth, hammering the S-wave flat.
   MECH: Use the harvest/distillery plus Lichess-SF-annotated games to actively mine the residual (positions where raw_v error is large: under-value at raw_v~0.7, over-value at raw_v~0.2, drift positions where M's value falls from peak), then fine-tune BT4 on this hard-negative set with a monotonicity-and-calibration loss, NOT a representative sample.
   HUGE: The capacity study showed the miscalibration is a specific nonlinear S-wave in raw_v alone; a small surgical hard-example-weighted fine-tune flattens exactly that curve where a broad retrain dilutes it, the cheapest path to fixing the named blind spots at the SOURCE rather than recalibrating downstream (which detuned the search).
#93 [THE EVALUATOR'S BLIND SPOTS â€” attack B] DECORRELATED DUAL-NET DISAGREEMENT SEARCH (wild:3) -- Two independently-trained evaluators whose DISAGREEMENT is itself the epistemic signal driving where to search.
   MECH: Train a second architecturally-different evaluator (NNUE-style or a distilled GM-transformer we own) on a decorrelated data slice; at each node compute |v_BT4 - v_other| as a free calibrated epistemic variance, gate max-backup by agreement, and resolve persistent disagreements with extra search, so the ensemble mean is more accurate AND the variance is real.
   HUGE: Ensembling is the one known method that reliably reduces value error without detuning, and it manufactures the trustworthy uncertainty the 5 failed reorganizers lacked; the ~19% eval-quality losses where a shallow SF-d10 ranks the move right are exactly the disagreement cases this catches near-free.
#94 [THE EVALUATOR'S BLIND SPOTS â€” attack B] TRAJECTORY-CONDITIONED EVAL (kill drift by evaluating the path, not the leaf) (wild:4) -- Evaluate a position conditioned on the move sequence that reached it, so noisy deep frontier leaves can't poison a good move.
   MECH: Give the evaluator a short move-history / plan embedding as input and train it on annotated games to predict OUTCOME-given-trajectory rather than static-position outcome; the lattice then backs up trajectory-consistent values, so a deep maneuvering line that BT4's static eval devalues at the frontier is judged by the whole plan, not one leaf.
   HUGE: Middlegame DRIFT (the ~31% search-side losses where more search DEVALUES the good move because max-backup trusts a noisy deep leaf as certain) is structurally a static-leaf pathology; a trajectory-aware eval makes the fixed point reflect the plan's payoff and dissolves the bad attractor the lattice settles into.
#95 [THE EVALUATOR'S BLIND SPOTS â€” attack B] MONOTONIC-BY-CONSTRUCTION VALUE NET (wild:4) -- Architecturally guarantee the eval is non-decreasing in decisiveness so the S-wave cannot exist.
   MECH: Wrap BT4's penultimate features in a learned monotonic network (positive-weight / Deep-Lattice-Network layers) constrained so value is monotone non-decreasing in a small set of decisiveness signals (won-tablebase distance, persistent material/king-safety advantage), trained to match deep labels only WITHIN the monotone function class.
   HUGE: The measured failure is exactly non-monotonicity: under-valuing clearly-winning, over-valuing slight edges. A net that CANNOT be non-monotonic in decisiveness eliminates the whole blind-spot class by architecture, not data coverage, and yields the won-position gradient for free.
#96 [THE EVALUATOR'S BLIND SPOTS â€” attack B] PROOF-ANCHORED BELIEF PROPAGATION (variance-0 truth bends the whole field) (wild:5) -- Treat machine-checked proofs and Syzygy as observed variables and run probabilistic inference that pulls the net's beliefs toward them across the transposition DAG.
   MECH: Model the lattice as a factor graph: net evals are noisy observations with the evidential variance head, proofs/tablebase results are variance-0 clamps; run loopy belief propagation / message passing over the DAG so one proven node re-weights the net's beliefs at thousands of correlated upstream positions, calibrating the S-wave online using proofs already in the persistent ledger.
   HUGE: STILLWATER uniquely has variance-0 anchors (proofs plus 3-7man tablebases) and a persistent DAG; using them not as local pins but as GLOBAL evidence that continuously re-calibrates the net's miscalibrated value field is a capability no tree engine can have, fixing the blind spots at INFERENCE time with no retrain.
#97 [SEARCH-NETWORK CO-DESIGN: lc0-on-BT4 ~36] Differentiable Relaxation (wild:5) -- Make the lattice fixed-point relaxation differentiable and train a learned backup operator by backprop to game outcomes.
   MECH: Relaxation converges to fixed point B* of operator T_theta. Replace its aggregation with a tiny GNN; train theta by implicit diff at the fixed point (DEQ phantom-gradient, no unrolling). Loss matches root value/ranking to outcomes; proven nodes are variance-0 constraints.
   HUGE: Replaces minimax-max (31% of decisive losses are backup-drift) with a gradient-found optimal operator. First lever that out-extracts MAX without swapping a co-tuned readout.
#98 [SEARCH-NETWORK CO-DESIGN: lc0-on-BT4 ~36] Visit-Field Surrogate (wild:4) -- Distill lc0's visit-distribution-on-BT4 into a learned readout the lattice computes from its belief field, then train it past lc0.
   MECH: lc0 plays argmax-VISITS; we read argmax-VALUE against the grain. Run lc0-on-BT4 over DAGs as teacher; train a head mapping belief features to that improved policy. Stage 1 imitates lc0; stage 2 trains on outcomes, exploiting DAG transposition reuse lc0 lacks.
   HUGE: Closes the ~170-Elo search-extraction gap to lc0 on the identical net, then surpasses it.
